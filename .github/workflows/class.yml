name: CI develop env

on:
  push:
    branches: [ "class" ]



jobs:
  test:
    runs-on: Ubuntu-20.04
    steps:
    - uses: actions/checkout@v3
    - name: Node yarn ${{ matrix.node-version }}
      run: echo ${{ vars.DEVELOP.TEST_VAR }}

